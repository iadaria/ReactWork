import React from 'react';
import { View, Dimensions, StyleSheet, TouchableOpacity, Text } from "react-native";

const FoodScreen = () => {
    return (
        <View>FoodScreen</View>
    );
};

export default FoodScreen;
